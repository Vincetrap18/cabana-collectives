// SEO metadata helpers for future expansion (schema, dynamic OG, etc.).
export function buildMetadata(_title: string, _description: string) {
  return {
    title: _title,
    description: _description
  };
}
